# 4. Replace the double space from problem 3 with single spaces.
a="Hassan  Ali"
print(a)
print(a.replace("  "," "))
# replace function make a new string and does not change the original string.
#means that our original string is still the same and we have a 
# new string with the changes that we want.   
# simply Strings are immuteable
# it will replace double space with single space in my string.
